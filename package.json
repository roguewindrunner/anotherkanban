{
    "name": "anotherkanban",
    "private": true,
    "devDependencies": {
      "@types/bun": "latest"
    },
    "peerDependencies": {
      "typescript": "^5"
    },
    "workspaces": [
        "apps/*",
        "packages/*"
    ],

    "scripts": {
        "dev": "echo \"We'll configure this later\"",
        "build": "echo \"todo\"",
        "lint": "echo \"todo\"",
        "test": "echo \"todo\""
    }
}
